# mwd-pickup system-level settings (applies to all instances in this system)

# Cross-instance shared network (meshnet)
MESHNET_NAME=mwd-pickup-mesh
MESHNET_EXTERNAL=true

# MW get_config() overlay (/root/config.5) - instance-level addressing
# These values are resolved via meshnet gateway aliases.
MASTER_EBUS=storer
MASTER_IP=storer
RETRIEVER_EBUS=retriever
RETRIEVER_IP=retriever

# Optional: pin gateway image
# GATEWAY_IMAGE=haproxy:2.9-alpine
